{% extends 'base.html.twig' %}

{% block body %}
    {% include 'reginfo/header.html.twig' %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-4 col-md-offset-4">
                <div class="row">
                    <div class="col-md-8 col-md-offset-2">
                        <div class="panel panel-default">
                            <div class="panel-heading"><img src="{{ asset('images/patient.png') }}"></div>
                            <div class="panel-body">
                                {{ form_start(form,  {'attr': {'novalidate': 'novalidate'}}) }}
                                {{ form_widget(form) }}
                                <input type="submit" value="Додати" class="btn btn-success btn-sm"/>
                                {{ form_end(form) }}
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="text-center">
    <ul class="list-group">
        <li>
            <a href="{{ path('reception_index') }}" class="btn btn-warning">Назад до списку</a>
        </li>
    </ul>
    </div>
{% endblock %}
