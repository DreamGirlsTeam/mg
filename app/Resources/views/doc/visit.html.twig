{% extends 'base.html.twig' %}
{% block stylesheets %}
    <link href="{{ asset('/bundles/framework/css/doctor.css') }}" rel="stylesheet"/>
{% endblock %}

{% block body %}
    {% include 'header.html.twig' %}
    <div class="col-md-3">
        <div class="panel panel-primary">
            <div class="panel-heading">
                <div class="col-md-offset-5">Дії</div>
            </div>
            <div class="panel-body">
                <ul class="list-group">
                    <li class="list-group-item">
                        <a href="{{ path('doctor', { 'actorId': actorId }) }}">На головну</a>
                    </li>
                    <li class="list-group-item">
                        <a href="{{ path('doctor_card', { 'actorId': actorId }) }}">Назад до картки</a>
                    </li>
                </ul>
            </div>
        </div>
    </div>
        <div class="col-md-4 col-md-offset-1">
            <div class="row">
                <div class="col-md-8 col-md-offset-2">
                    <div class="panel panel-default">
                        <div class="panel-body">
                            {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }}
                            {{ form_widget(form) }}{#
                            <input type="submit" value="Створити" class="btn btn-success" onclick="radioChecked()"/>#}
                            {{ form_end(form) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
{% endblock %}

{% block javascripts %}
    <script src="{{ asset('bundles/framework/js/jQuery/jquery-1.12.3.min.js') }}"></script>
    <script src="{{ asset('bundles/framework/js/upload.js') }}"></script>
{% endblock %}