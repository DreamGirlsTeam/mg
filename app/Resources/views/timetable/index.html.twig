{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="https://cdn.datatables.net/1.10.11/css/jquery.dataTables.min.css"/>
    <link rel="stylesheet" href="https://cdn.datatables.net/1.10.11/css/dataTables.bootstrap.min.css"/>
{% endblock %}

{% block body %}
    {% include 'header.html.twig' %}
    <div class="container-fluid">
        <div class="row">
            <div class="col-md-3">
                <div class="panel panel-primary">
                    <div class="panel-heading">
                        <div class="col-md-offset-5">Дії</div>
                    </div>
                    <div class="panel-body">
                        <ul class="list-group">
                            <li class="list-group-item">
                                <a href="{{ path('reception_timetable_new') }}">Додати години роботи</a>
                            </li>
                            <li class="list-group-item">
                                <a href="{{ path('reception_index') }}">Назад до списку</a>
                            </li>
                        </ul>
                    </div>
                </div>
            </div>
            <div class="col-md-6">
                <div class="well" style="border-color: #EBF5FB; background-color:#F8F9F9;">
                    <table class="table table-bordered table-hover">
                        <thead>
                        <tr>
                            <th> <div class="col-md-offset-5">Лікарі</div></th>
                        </tr>
                        </thead>
                        <tbody>
                        {% for timetable in timetables %}
                            <tr>
                                <td>
                                    <h4>{{ names[loop.index - 1] }}</h4>
                                    {{ timetable.beginTime|date('H:i:s') }} - {{ timetable.endTime|date('H:i:s') }}
                                    <h4><small><b>Середній час візиту: {{ timetable.averTime }} хвилин</b></small></h4>
                                    <a href="{{ path('reception_timetable_edit', { 'id': timetable.id }) }}">Редагувати</a>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
